
---

jobs:
  - name: concourse
    templates:
      - name: atc
        properties:
          github_auth:
            authorize:
              - (( append ))
              - user: bandesz
              - user: camelpunch
