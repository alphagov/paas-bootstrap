---

instance_groups:
  - name: concourse
    jobs:
      - name: web
        properties:
          github_auth:
            client_id: (( grab $GITHUB_CLIENT_ID ))
            client_secret: (( grab $GITHUB_CLIENT_SECRET ))
          main_team:
            auth:
              config: |+
                roles:
                - name: owner
                  local:
                    users:
                    - admin
                  github:
                    users:
                    - AP-Hunt
                    - cadmiumcat
                    - dark5un
                    - kr8n3r
                    - jackjoy-gds
                    - LeePorte
                    - nimalank7
                    - risicle
                    - schmie
                    - whi-tw
                    - whpearson
                - name: pipeline-operator
                  github:
                    users:
